import { NavLink } from "react-router-dom";

const Tweets = () => {
    return (
        <>
            <h1>Tweets</h1>
            <NavLink to="/">Back</NavLink>
        </>);
};

export default Tweets;